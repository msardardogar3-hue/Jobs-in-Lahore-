import React, { useState } from "react"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Briefcase, LogIn, Search, User } from "lucide-react";

export default function JobPortal() { const [loggedIn, setLoggedIn] = useState(false);

return ( <div className="min-h-screen bg-gray-100 p-6"> {/* Header */} <header className="flex justify-between items-center mb-6"> <h1 className="text-3xl font-bold flex items-center gap-2"> <Briefcase /> Job Opportunities </h1> <Button onClick={() => setLoggedIn(true)} className="flex gap-2"> <LogIn /> Sign in with Google </Button> </header>

{/* Search */}
  <Card className="mb-6">
    <CardContent className="p-4 flex gap-2">
      <Input placeholder="Search jobs by title or skill" />
      <Button><Search /></Button>
    </CardContent>
  </Card>

  {/* Job Listings */}
  <div className="grid md:grid-cols-3 gap-4">
    {[1,2,3,4,5,6].map(job => (
      <Card key={job} className="hover:shadow-lg transition">
        <CardContent className="p-4">
          <h2 className="text-xl font-semibold">Customer Support Agent</h2>
          <p className="text-sm text-gray-600">Lahore · Full Time</p>
          <p className="mt-2 text-sm">Good communication skills required. Freshers can apply.</p>
          <Button className="mt-4 w-full">Apply Now</Button>
        </CardContent>
      </Card>
    ))}
  </div>

  {/* Profile Section */}
  {loggedIn && (
    <Card className="mt-8">
      <CardContent className="p-4">
        <h2 className="text-2xl font-bold flex items-center gap-2"><User /> My Profile</h2>
        <p className="text-sm text-gray-600">Signed in with Google</p>
        <ul className="mt-3 list-disc list-inside text-sm">
          <li>Saved Jobs</li>
          <li>Applied Jobs</li>
          <li>Upload CV</li>
          <li>Job Alerts</li>
        </ul>
      </CardContent>
    </Card>
  )}

  {/* Footer */}
  <footer className="mt-10 text-center text-sm text-gray-500">
    © 2026 Job Opportunities Portal · Easy Jobs for Everyone
  </footer>
</div>

); }
